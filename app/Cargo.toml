[package]
name = "contract-app"
version = "0.1.0"
edition = "2021"

[dependencies]
sails-rs.workspace = true
gstd.workspace = true

[dev-dependencies]
sails-rs = { workspace = true, features = ["gstd", "std"] }
tokio.workspace = true